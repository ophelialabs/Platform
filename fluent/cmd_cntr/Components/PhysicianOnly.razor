@using cmd_cntr.Services
@inject AuthService AuthService

@if (AuthService.HasRole("physician"))
{
    @ChildContent
}

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
